int count=0;
		for(int i=0;i<nums.length;i++)
		{
			if(nums[i]!=0)
			{
		
			nums[count++]=nums[i];
		     }
			
		}
		System.out.println(count);
		while(count<nums.length)
		{
			nums[count++]=0;
		}
		for(int i=0;i<nums.length;i++)
		{
			System.out.print(nums[i]+" ");
		}
